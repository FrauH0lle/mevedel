;;; test-foo.el --- Hello world function -*- lexical-binding: t -*-

(defun hello-europe ()
  "Print a friendly greeting."
  (interactive)
  (message "Hello, Vietnam!!"))

(provide 'test-foo)
;;; test-foo.el ends here
