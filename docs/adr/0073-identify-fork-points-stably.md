# Identify fork points independently of turn numbers

Conversation variants relate through a stable fork-point identity attached to the settled assistant response, with the cumulative turn retained only as descriptive metadata. True in-place Rewind can discard a Source response and later reuse its turn number while child sessions survive, so turn-based lineage could otherwise reconnect detached Forks to an unrelated new response.
